

//if GAME = PLAYING (NO WINNER)
    // if TURN = PLAYER && cell is AVAILABLE
        // select cells and add score to player
    // then TURN = BOT && cell is AVAILABLE
        // select cells and add score to player
//else GAME = OVER (WINNER)
    // then RESET PLAYER and BOT scores and CELLS
  
    


//GameCard nows the STATE of the TABLE (later maybe> and renders only cells which need to be updated)
